### Application to rescue and adopt homeless animals

#### Installing Cordova
* `npm install -g cordova`

#### Installing from source
* `cd rescute`
* Add Platform: `cordova platform add android`
* To build: `cordova build android`
